<Window x:Class="WpfOcrInvoiceExtractor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfOcrInvoiceExtractor"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow"  Left="0" Top="0" d:DesignHeight="264.697" d:DesignWidth="270.281">
    <Grid Background="Gray">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20" Grid.Row="0">
            <Button Name="NewImportButton" Click="NewImportButton_Click">New Import</Button>
            <Button>Settings</Button>
        </StackPanel>

        <ItemsControl Name="templateList" Grid.Row="1">

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="Gray" BorderBrush="GhostWhite" BorderThickness="3" Margin="15,15,15,15">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal" Visibility="{Binding HideButtons}">
                                <Button Click="Edit_Template">Edit</Button>
                                <Button Click="Delete_Template">Delete</Button>
                            </StackPanel>
                            <Image Source="{Binding Display}" Height="200" MouseDown="Template_Click"></Image>
                            <TextBlock Text="{Binding Path=Vendor.DisplayName}" FontSize="14" FontWeight="Bold"></TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>

</Window>
