<Window x:Class="WpfOcrInvoiceExtractor.VendorSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfOcrInvoiceExtractor"
        mc:Ignorable="d"
        Title="VendorSelectDialog" Height="120" Width="300">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
            <Label VerticalAlignment="Center">Vendor:</Label>
            <ComboBox Name="vendorBox"></ComboBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right">
            <Button Name="okButton" Click="okButton_Click" IsDefault="True" Width="70" Margin="0,10,5,0">OK</Button>
            <Button Name="cancelButton" IsCancel="True" Width="70" Margin="0,10,5,0">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
